// Shown openly on the signup form (test version only). The real gate is
// the check inside the handle_new_user trigger in the database —
// keep this value in sync with instructor_secret_code() there.
export const INSTRUCTOR_SECRET_CODE = 'LUNARIA-TEACH';
